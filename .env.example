# 复制成 .env 再填。.env 已在 .gitignore 里，不会被提交。
#
#   cp .env.example .env
#   node scripts/check-provider.mjs            # 列出可选的服务商
#   node scripts/check-provider.mjs deepseek   # 用它跑一遍
#
# 只有这一个要填。协议、Endpoint、默认模型都从扩展自己那份
# PROVIDER_PRESETS 里取，不在这儿重复一遍（重复了就会走散）。
NAI_API_KEY=
